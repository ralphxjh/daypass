# DayPass · 税务居住期计算器

一款专为跨国穿梭的商务人士、数字游民和外派员工设计的纯本地、零成本 PWA 工具。帮助你精准追踪每年在不同国家的停留天数，直观预警税务居民身份风险。

🌐 在线体验 Demo | 📱 手机浏览器打开可添加至主屏幕，体验原生 APP 效果

✨ 核心功能
精准日期计算：支持跨年行程自动拆分，严格按照单自然年（1月1日-12月31日）计算实际停留天数。支持同日双算（入境日与出境日同天计为2天）。
护照印章式状态卡：每个国家是一页"护照"，停留天数像盖章一样累积，状态做成歪斜的朱红入境章——SAFE 安全 / WATCH 警告 / ALERT 危险，无需看数字即可一眼判断风险。卡片底部还有仿护照机读码（MRZ）行。
自定义国家与阈值：内置中/日/新三国模板，支持自由添加任意国家，并自定义税务居民判定天数阈值（如 183天、365天等）。
“仍在境内”支持：处理未出境的现实场景，系统自动以“今天”作为出境日期进行动态计算。
数据完全本地化：所有数据仅存储在浏览器 localStorage 中，绝不上云，保护敏感行程隐私。
一键导入/导出：支持将数据导出为 JSON 文件备份，支持从文件恢复（已完美兼容 iOS/安卓文件读取机制），换机无忧。
PWA 离线可用：安装至主屏幕后，即使断网也能正常打开和使用。

🎨 视觉设计（护照主题）
整体采用"护照"视觉语言，与"跨境停留"主题高度契合：
- 配色：护照封面墨绿 (#16302a) 作页面底色、牛皮纸 (#ece2c9) 作卡片内页、朱红 (#bf3b2b) 作印章/危险色、烫金 (#b07d28) 作警告色、墨绿 (#1f3d33) 作主文字。
- 字体：标题用 Noto Serif SC 衬线大字，天数与机读码用 Space Mono / IBM Plex Mono 等宽字，正文用 Noto Sans SC（通过 Google Fonts 引入；离线时自动降级为系统字体，不影响使用）。
- 主题样式集中在 src/index.css，并覆盖了 Ant Design Mobile 的主题变量。
- 免责提示：主页底部及各弹窗底部均常驻"不构成税务建议、请咨询专业税务师"的免责文字。

🛠 技术栈
前端框架：React 18
构建工具：Vite
UI 组件库：Ant Design Mobile (v5)
日期处理：Day.js
PWA 支持：vite-plugin-pwa (Workbox)
部署平台：Vercel (免费托管)

📂 项目架构
项目采用典型的 React Hooks 驱动架构，核心逻辑与视图分离：

```
src/
├── components/
│   ├── TaxStatusCard.jsx        # 核心护照状态卡（入境章 + 墨水斜纹进度条 + 机读码）
│   └── TaxStatusCard.module.css # 卡片局部样式
├── utils/
│   └── calculator.js            # 核心计算引擎（跨年拆分、天数累加）
├── App.jsx                      # 主界面与全局状态管理
├── index.css                    # 护照主题全局样式（配色令牌 + antd-mobile 主题覆盖）
└── main.jsx                     # 入口文件与 PWA Service Worker 注册
```
核心计算逻辑说明 (calculator.js)：
采用 effectiveStart 和 effectiveEnd 截断算法，将所有行程与目标年份求交集，确保跨年行程不会误算到非当年天数。计算公式为：天数 = 交集结束日 - 交集开始日 + 1（同日双算）。

📱 如何安装到手机桌面（PWA功能）
本应用支持作为 PWA (渐进式Web应用) 安装到手机主屏幕，获得类似原生 APP 的全屏体验：

iOS (Safari)：点击底部分享图标 ➔ 向下滑动找到并点击“添加到主屏幕”。
Android (Chrome)：浏览器会自动弹出“添加到主屏幕”底栏，或点击右上角菜单手动添加。
⚠️ 注意：必须通过 HTTPS 链接访问时，安装功能才会生效。

⚠️ 免责声明与限制
仅供参考，不构成税务建议：本工具仅基于单自然年物理停留天数进行数学计算，税务居民的判定涉及诸多复杂法律因素（如住所、习惯性居所、高管身份等），请以当地税法及专业税务师意见为准。
单一年份模型：目前系统采用“单自然年”计算模型，完美适配中、新等国的 183天规则。但对于如美国的实质居住测试（当年+前两年加权）等滚动周期规则，本工具无法自动适配，需用户自行评估。
浏览器隐私模式：在无痕/隐私模式下使用，localStorage 可能会在关闭窗口后被清空，且 PWA 离线功能受限，请使用常规浏览器模式。
